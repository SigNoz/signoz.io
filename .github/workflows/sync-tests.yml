name: Sync Tests

on:
  pull_request:
    paths:
      - "scripts/cms-sync/**"
      - "scripts/sync-content-to-strapi.js"
      - "scripts/check-cms-assets.js"
      - "tests/sync/**"
      - "tests/customers-data.test.js"
      - "data/case-study/**"
      - "app/(site)/customers/customersPageContent.ts"
      - "components/Customers/Customers.types.ts"
      - ".github/workflows/sync-tests.yml"

jobs:
  run-sync-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "20"

      - name: Install dependencies
        run: yarn install --frozen-lockfile --non-interactive

      - name: Run sync tests workflow
        run: yarn test:sync

      - name: Run customers data guard
        run: yarn test:customers-data
