import OSSToCloud from './OssToCloud'

import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'OSS to Cloud',
}

export default function OSSToCloudPage() {
  return <OSSToCloud />
}