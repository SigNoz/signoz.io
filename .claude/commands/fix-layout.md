# Fix Layout

Use the `signoz-visual-review` skill focusing on layout and spacing issues.

Consult the layout-spacing.md reference for:
- Spacing scale adherence (no arbitrary values)
- Section padding rhythm consistency
- Group spacing vs inner spacing
- Split section alternation pattern
- Grid layout column choices
- Responsive grid collapse behavior

$ARGUMENTS
