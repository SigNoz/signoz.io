.tags {
  display: flex;
  align-items: center;
  gap: 8px;
}

.tag {
  border: 1px solid #f99781; /* Soft coral border */
  padding: 2px 10px; /* Slightly wider padding for better balance */
  border-radius: 6px; /* Rounder corners for a smoother look */
  font-size: 12px;
  color: #f99781;
  font-weight: 500; /* Medium weight for a cleaner appearance */
  text-decoration: none;
  transition: transform 0.2s ease; /* Subtle transition for interactivity */
}

.tag:hover {
  transform: scale(1.05); /* Slightly enlarge on hover for emphasis */
  cursor: pointer;
}
