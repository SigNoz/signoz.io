'use client'

// Constants with more specific names to avoid conflicts with analytics
export const ANONYMOUS_ID_KEY = 'gb_user_anonymous_id'
export const ANONYMOUS_ID_COOKIE = 'gb_anonymous_id'
export const COOKIE_EXPIRY_DAYS = 365

// This hook is now handled directly by the AnonymousIdSetter component
