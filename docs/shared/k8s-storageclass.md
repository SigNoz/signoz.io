In case, you would like to use your own storage class, you can unset/remove
`installCustomStorageClass` and set `storageClass` to desired storage class.

:::info
To list storage class in your Kubernetes cluster: `kubectl get storageclass`.
:::