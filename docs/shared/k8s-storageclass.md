In case, you would like to use your own storage class, you can remove `installCustomStorageClass` and replace `storageClass` to desired storage class from above YAML.

:::info
To list storage class in your Kubernetes cluster: `kubectl get storageclass`.
:::