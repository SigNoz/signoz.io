---
date: 2025-05-12
title: Third Party API Monitoring
id: overview
hide_table_of_contents: false
---

## Overview

The **API Monitoring** feature helps you track all the APIs your internal services are calling.

It uses span attributes like
- `net.peer.name`
- `http.url`
- `http.target`
to automatically derive API calls.

## Instrumentation Requirements

To enable API monitoring:

- Ensure your application is instrumented correctly.
- For every **external call**, spans must include:
  - `net.peer.name`
  - `http.url`
  - `http.target`

> If you are using manual instrumentation, make sure these attributes are added explicitly for each outbound request.

### Third Party API Monitoring Home page

The product view provides a breakdown of:

- All **domains** (internal & external)
- Number of active **endpoints**
- **Error rates**
- **Latency**

You can **hide internal IPs** from landing page using Show IP addresses toggle.

<figure data-zoomable align='center'>
    <img src="/img/docs/third-party-api/api-monitoring-landing-page.gif" alt="Third-Party-API monitoring"/>
    <figcaption><i>Third Party API monitoring landing page</i></figcaption>
</figure>

### Exploring Endpoints

You can filter out domains list based on the quick filters on left panel or using the query builder on top

<figure data-zoomable align='center'>
    <img src="/img/docs/third-party-api/endpoint-explore-part1.gif" alt="Third Party API monitoring endpoint detail"/>
    <figcaption><i>Third Party API monitoring endpoint explore</i></figcaption>
</figure>

Clicking on a specific domain lets you explore its endpoints, giving an overview for the selected time range. And by going to the Endpoint(s) Stats tab directly without clicking on any endpoint will show the stats for the select domain.

<figure data-zoomable align='center'>
    <img src="/img/docs/third-party-api/endpoint-explore-part2.gif" alt="Third Party API monitoring endpoint detail"/>
    <figcaption><i>Third Party API monitoring endpoint explore</i></figcaption>
</figure>

On the All Endpoints tab you can:

- Group by various attributes to analyze patterns
- Apply filters from the query builder filter search.

<figure data-zoomable align='center'>
    <img src="/img/docs/third-party-api/endpoint-explore-part3.gif" alt="Third Party API monitoring endpoint detail"/>
    <figcaption><i>Third Party API monitoring endpoint explore</i></figcaption>
</figure>

### Endpoint Details

Once you select an endpoint, you’ll see detailed metrics for that specific endpoint such as:

- Response status breakdown (grouped by status codes)
- Latency metrics
- Operations per second (ops/sec)

<figure data-zoomable align='center'>
    <img src="/img/docs/third-party-api/endpoint-details-part1.gif" alt="Third Party API monitoring endpoint detail"/>
    <figcaption><i>Third Party API monitoring endpoint details</i></figcaption>
</figure>

You can click on any bar in the graph to view the corresponding traces.

<figure data-zoomable align='center'>
    <img src="/img/docs/third-party-api/endpoint-details-part2.gif" alt="Third Party API monitoring endpoint detail"/>
    <figcaption><i>Third Party API monitoring endpoint details</i></figcaption>
</figure>

### Latency Analysis

Clicking on the latency graph displays latency grouped by status codes.
You can click on any point in the graph to view the corresponding traces.

<figure data-zoomable align='center'>
    <img src="/img/docs/third-party-api/endpoint-latency.png" alt="Third Party API monitoring endpoint latency"/>
    <figcaption><i>Third Party API monitoring endpoint latency</i></figcaption>
</figure>

### Rate Over Time

The dashboard also provides a Rate Over Time chart with clickable navigation to associated traces. You can click on any point in the graph to view the corresponding traces.

<figure data-zoomable align='center'>
    <img src="/img/docs/third-party-api/rate-over-time.png" alt="Third Party API monitoring rate over time"/>
    <figcaption><i>Third Party API monitoring rate over time</i></figcaption>
</figure>

### P99 Latency

The P99 latency metrics can be tracked over time, allowing you to investigate specific traces for deeper analysis.

<figure data-zoomable align='center'>
    <img src="/img/docs/third-party-api/endpoint-p99latency.png" alt="Third Party API monitoring endpoint p99latency"/>
    <figcaption><i>Third Party API monitoring endpoint p99 latency</i></figcaption>
</figure>

### Dependent Services

You can view dependent services along with their call counts, average latency, and error rates. Clicking any service will take you to the services dashboard.
<figure data-zoomable align='center'>
    <img src="/img/docs/third-party-api/endpoint-services.gif" alt="Third Party API monitoring endpoint services"/>
    <figcaption><i>Third Party API monitoring endpoint services</i></figcaption>
</figure>

### Top Errors

You can view errors with or without status messages in the Top Errors tab for either a domain or specific endpoint.
<figure data-zoomable align='center'>
    <img src="/img/docs/third-party-api/endpoint-top-errors.gif" alt="Third Party API monitoring endpoint top errors"/>
    <figcaption><i>Third Party API monitoring endpoint top errors</i></figcaption>
</figure>
