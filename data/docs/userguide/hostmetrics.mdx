---
date: 2026-01-10
title: Collect Host Metrics with OpenTelemetry
id: hostmetrics
description: Collect host metrics like CPU, memory, disk, and network from your servers using the OpenTelemetry hostmetrics receiver and send them to SigNoz.
doc_type: howto
hide_table_of_contents: true
---

import GetHelp from '@/components/shared/get-help.md'

Host metrics are fundamental performance indicators collected directly from a system's operating system. These metrics provide granular visibility into the physical or virtual server's resource utilization and health status.

The OpenTelemetry hostmetrics receiver collects metrics on CPU, memory, disk, network, paging, swap, and system performance.

<KeyPointCallout title="Using self-hosted SigNoz?" defaultCollapsed={true}>
  Most steps are identical. To adapt this guide, update the endpoint and remove the ingestion key
  header as shown in [Cloud →
  Self-Hosted](https://signoz.io/docs/ingestion/cloud-vs-self-hosted/#cloud-to-self-hosted).
</KeyPointCallout>

<Tabs entityName="platform">

<TabItem value="vm" label="Virtual Machine" default>

<KeyPointCallout title="What classifies as VM?" defaultCollapsed={true}>
A VM is a virtual computer that runs on physical hardware. This includes:
- **Cloud VMs**: AWS EC2, Google Compute Engine, Azure VMs, DigitalOcean Droplets
- **On-premise VMs**: VMware, VirtualBox, Hyper-V, KVM
- **Bare metal servers**: Physical servers running Linux/Unix directly

Use this section if you're deploying your application directly on a server or VM without containerization.

</KeyPointCallout>

## Prerequisites

To collect host metrics, you must have the OpenTelemetry Collector installed on your virtual machine.

If you haven't installed it yet, follow the [OpenTelemetry Collector Installation Guide](https://signoz.io/docs/opentelemetry-collection-agents/get-started/) to install the collector binary or run it as a service.

## Step 1: Configure the Collector

Once the collector is installed, you need to configure it to collect host metrics and send them to SigNoz.

1.  **Locate your `config.yaml` file.**

    - **Linux (Default):** `/etc/otelcol-contrib/config.yaml`
    - **Manual Binary:** In the directory where you run the binary.

2.  **Update `config.yaml`** with the following configuration based on your setup.

Replace `<region>` and `<SIGNOZ_INGESTION_KEY>` with your actual values.

```yaml
receivers:
  hostmetrics:
    collection_interval: 60s
    scrapers:
      cpu: {}
      disk: {}
      load: {}
      filesystem: {}
      memory: {}
      network: {}
      nfs: {} # Linux only
      paging: {}
      process:
        mute_process_name_error: true
        mute_process_exe_error: true
        mute_process_io_error: true
      processes: {}
      system: {}

processors:
  batch:
  resourcedetection:
    detectors: [env, system]

exporters:
  otlp:
    endpoint: 'ingest.<region>.signoz.cloud:443'
    tls:
      insecure: false
    headers:
      'signoz-ingestion-key': '<SIGNOZ_INGESTION_KEY>'

service:
  pipelines:
    metrics:
      receivers: [hostmetrics]
      processors: [resourcedetection, batch]
      exporters: [otlp]
```

- **Region:** Your SigNoz Cloud region (e.g., `us`, `eu`, `in`).
- **Ingestion Key:** Find it in **Settings > Ingestion Settings** in the SigNoz UI.

## Step 2: Run the Collector

After saving the configuration, restart the collector service to apply changes.

### Linux (Systemd)

```bash
sudo systemctl restart otelcol-contrib
sudo systemctl status otelcol-contrib
```

### Manual Binary

If you are running the binary manually (e.g., for testing):

```bash
./otelcol-contrib --config config.yaml
```

</TabItem>
<TabItem value="docker" label="Docker">

## Prerequisites

To collect host metrics from a Docker container, you must bind mount the host filesystem to the container. This allows the collector to read system statistics from the host.

## Step 1: Configure the Collector

1.  **Create a `config.yaml` file.**

2.  **Add the `root_path` configuration** to the `hostmetrics` receiver. This tells the receiver where to find the host's filesystem inside the container.

Replace `<region>` and `<SIGNOZ_INGESTION_KEY>` with your actual values.

```yaml
receivers:
  hostmetrics:
    root_path: /hostfs
    collection_interval: 60s
    scrapers:
      cpu: {}
      disk: {}
      load: {}
      filesystem: {}
      memory: {}
      network: {}
      nfs: {} # Linux only
      paging: {}
      process:
        mute_process_name_error: true
        mute_process_exe_error: true
        mute_process_io_error: true
      processes: {}
      system: {}

processors:
  batch:
  resourcedetection:
    detectors: [env, system]

exporters:
  otlp:
    endpoint: 'ingest.<region>.signoz.cloud:443'
    tls:
      insecure: false
    headers:
      'signoz-ingestion-key': '<SIGNOZ_INGESTION_KEY>'

service:
  pipelines:
    metrics:
      receivers: [hostmetrics]
      processors: [resourcedetection, batch]
      exporters: [otlp]
```

- **Region:** Your SigNoz Cloud region (e.g., `us`, `eu`, `in`).
- **Ingestion Key:** Find it in **Settings > Ingestion Settings** in the SigNoz UI.

## Step 2: Run the Collector

Run the collector container with the `--volume` flag to mount the host root directory to `/hostfs`.

```bash
docker run -d \
  --name otelcol-hostmetrics \
  --volume /:/hostfs:ro \
  --volume $(pwd)/config.yaml:/etc/otelcol-contrib/config.yaml \
  otel/opentelemetry-collector-contrib:latest \
  --config=/etc/otelcol-contrib/config.yaml
```

</TabItem>
<TabItem value="k8s" label="Kubernetes Nodes">

## Prerequisites

To collect host metrics from your Kubernetes nodes, you need to have the SigNoz `k8s-infra` Helm chart installed on your Kubernetes cluster.

If you haven't installed it yet, follow the [Kubernetes Infrastructure Metrics Guide](https://signoz.io/docs/tutorial/kubernetes-infra-metrics/) to deploy the chart.

## Step 1: Verify Host Metrics Collection

Host metrics collection is enabled by default when using the `k8s-infra` Helm chart. The chart deploys an agent on every node to gather and forward system metrics.

## Step 2: Customize Collection (Optional)

You can customize host metrics collection via the `hostMetrics` preset in your `values.yaml`.

```yaml
hostMetrics:
  collectionInterval: 60s
  scrapers:
    cpu:
      metrics:
        system.cpu.logical.count:
          enabled: true
```

</TabItem>
<TabItem value="windows" label="Windows">

## Prerequisites

To collect host metrics on Windows, you must have the OpenTelemetry Collector installed on your Windows server.

If you haven't installed it yet, follow the [OpenTelemetry Collector Installation Guide](https://signoz.io/docs/opentelemetry-collection-agents/get-started/) to install the collector as a Windows service.

## Step 1: Configure the Collector

1.  **Locate your `config.yaml` file.**

    - **Default:** `C:\Program Files\OpenTelemetry Collector\config.yaml`

2.  **Update `config.yaml`** with the following configuration.

Replace `<region>` and `<SIGNOZ_INGESTION_KEY>` with your actual values.

```yaml
receivers:
  hostmetrics:
    collection_interval: 60s
    scrapers:
      cpu: {}
      disk: {}
      load: {}
      filesystem: {}
      memory: {}
      network: {}
      paging: {}
      process:
        mute_process_name_error: true
        mute_process_exe_error: true
        mute_process_io_error: true
      system: {}

processors:
  batch:
  resourcedetection:
    detectors: [env, system]

exporters:
  otlp:
    endpoint: 'ingest.<region>.signoz.cloud:443'
    tls:
      insecure: false
    headers:
      'signoz-ingestion-key': '<SIGNOZ_INGESTION_KEY>'

service:
  pipelines:
    metrics:
      receivers: [hostmetrics]
      processors: [resourcedetection, batch]
      exporters: [otlp]
```

- **Region:** Your SigNoz Cloud region (e.g., `us`, `eu`, `in`).
- **Ingestion Key:** Find it in **Settings > Ingestion Settings** in the SigNoz UI.

## Step 2: Run the Collector

After saving the configuration, restart the collector service to apply changes.

```powershell
Restart-Service otelcol-contrib
Get-Service otelcol-contrib
```

</TabItem>
</Tabs>

## Advanced Configuration

### Configure Different Frequencies

You can configure different collection intervals for different metrics by defining multiple `hostmetrics` receivers. This is useful if you want frequently changing metrics (like CPU/Memory) to be collected more often than stable ones (like Filesystem).

```yaml
receivers:
  # Receiver for high-frequency metrics (e.g., every 10s)
  hostmetrics/fast:
    collection_interval: 10s
    scrapers:
      cpu: {}
      memory: {}

  # Receiver for low-frequency metrics (e.g., every 1m)
  hostmetrics/slow:
    collection_interval: 60s
    scrapers:
      filesystem: {}
      disk: {}
      network: {}

service:
  pipelines:
    metrics:
      receivers: [hostmetrics/fast, hostmetrics/slow]
      # ... other processors and exporters
```

## Verification

Once configured, verify that metrics are flowing to SigNoz.

1.  Go to the **Infrastructure Monitoring** section in SigNoz (Hosts tab).
2.  You should see your host listed with metrics like CPU and Memory usage.

<Figure
  src="/img/docs/infrastructure-monitoring/host-setup-output.webp"
  alt="Host List in SigNoz Infrastructure Monitoring"
  caption="Host List in SigNoz Infrastructure Monitoring"
/>

<details>
<ToggleHeading>

## Troubleshooting

</ToggleHeading>

### No metrics appearing in SigNoz

**Symptoms:** Host not visible in Infrastructure Monitoring > Hosts tab.

**Resolution:**

1. Check collector logs for errors:
   ```bash
   journalctl -u otelcol-contrib -f
   ```
2. Verify the hostmetrics receiver is enabled in your config.
3. Ensure the collector can reach the SigNoz endpoint (port 443 for Cloud, 4317 for Self-Hosted).

### Connection Refused

**Symptoms:** Logs show connection refused errors.

**Resolution:**

- Ensure the OTLP endpoint is reachable.
- Verify the correct port is being used.
- Check firewall settings.

### Invalid Key (Cloud)

**Symptoms:** HTTP 401 Unauthorized errors in logs.

**Resolution:**

- Double-check your ingestion key in `config.yaml`.
- Ensure it matches the key in SigNoz (Settings > Ingestion Settings).

</details>

## Next Steps

- [Visualize Host Metrics using Dashboard Templates](https://signoz.io/docs/userguide/dashboard-templates/)
- [Set up Alerts for Host Metrics](https://signoz.io/docs/userguide/alerts-management/)
- [Learn more about Infrastructure Monitoring](https://signoz.io/docs/userguide/infrastructure-monitoring/)

## Get Help

<GetHelp />
