---
date: 2025-11-25
id: opentelemetry-envoy-metrics
title: Send Envoy Metrics to SigNoz using OpenTelemetry
description: Send metrics from Envoy to SigNoz using the OpenTelemetry Collector.
tags: [SigNoz Cloud, Self-Host]
doc_type: howto
---

import GetHelp from '@/components/shared/get-help.md'

## Overview

Envoy is a high-performance edge and service proxy. This guide will help you send metrics from Envoy to SigNoz using the OpenTelemetry (OTel) Collector.

## Prerequisites

- **SigNoz OTel Collector installed**: You need the OpenTelemetry Collector running in your environment. See [Installation Guides](https://signoz.io/docs/opentelemetry-collection-agents/get-started/).
- **Access to Configuration**: You must be able to edit the `otel-collector-config.yaml` file where the collector is running.

## Setup

### Step 1: Configure Envoy with OpenTelemetry Sink

Envoy can send metrics to SigNoz using the OpenTelemetry (OTel) protocol. You can learn more about it at [OpenTelemetry Stat Sink](https://www.envoyproxy.io/docs/envoy/latest/operations/operations#operations-metrics).

Update your Envoy configuration:

```yaml:envoy.yaml
stats_sinks:
  - name: envoy.stat_sinks.open_telemetry
    typed_config:
      "@type": type.googleapis.com/envoy.extensions.stat_sinks.open_telemetry.v3.SinkConfig
      grpc_service:
        envoy_grpc:
          cluster_name: opentelemetry_collector
      # Optional: emit histogram metrics
      # report_histograms_as_deltas: true
      # report_counters_as_deltas: true

static_resources:
  clusters:
    - name: opentelemetry_collector
      type: STRICT_DNS
      lb_policy: ROUND_ROBIN
      typed_extension_protocol_options:
        envoy.extensions.upstreams.http.v3.HttpProtocolOptions:
          "@type": type.googleapis.com/envoy.extensions.upstreams.http.v3.HttpProtocolOptions
          explicit_http_config:
            http2_protocol_options: {}
      load_assignment:
        cluster_name: opentelemetry_collector
        endpoints:
          - lb_endpoints:
              - endpoint:
                  address:
                    socket_address:
                      address: otel-collector # Update with your collector address
                      port_value: 4317
```

### Step 2: Configure OpenTelemetry Collector

Update your OpenTelemetry Collector configuration in `otel-collector-config.yaml`:

```yaml:otel-collector-config.yaml
receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318

processors:
  batch:

  memory_limiter:
    check_interval: 5s
    limit_mib: 4000

exporters:
  otlp:
    endpoint: "ingest.{region}.signoz.cloud:443"
    tls:
      insecure: false
    headers:
      "signoz-ingestion-key": "<SIGNOZ_INGESTION_KEY>"

service:
  pipelines:
    metrics:
      receivers: [otlp]
      processors: [memory_limiter, batch]
      exporters: [otlp]
```

Replace the placeholders:

- `<region>`: Set to your SigNoz Cloud [region](https://signoz.io/docs/ingestion/signoz-cloud/overview/#endpoint).
- `<SIGNOZ_INGESTION_KEY>`: Ingestion key for SigNoz Cloud, see [Ingestion Keys](https://signoz.io/docs/ingestion/signoz-cloud/keys/) for more information.

<Admonition type="info">
  Using self-hosted SigNoz? Most steps are identical. To adapt this guide, update the endpoint and
  remove the ingestion key header as shown in [Cloud →
  Self-Hosted](https://signoz.io/docs/ingestion/cloud-vs-self-hosted/#cloud-to-self-hosted).
</Admonition>

### Step 3: Restart the Collector

Restart the OpenTelemetry Collector for the changes to take effect.

## Validate Metrics

After setting up the collector, go to the **[Metrics Explorer](https://signoz.io/docs/metrics-management/metrics-explorer/)** in SigNoz and search for a metric from your application, run the query to confirm data is flowing:

- `envoy_cluster_upstream_cx_total`
- `envoy_cluster_upstream_cx_active`

## Next Steps

- **Create Dashboards**: Visualize your Envoy metrics by creating custom dashboards. [Learn more](https://signoz.io/docs/userguide/dashboards/).
- **Set Alerts**: Configure alerts to get notified when metrics cross a threshold. [Learn more](https://signoz.io/docs/alerts-management/metrics-based-alerts/).

## Get Help

<GetHelp />
