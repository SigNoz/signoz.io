---
date: 2026-05-15
id: query-builder

title: Query Builder
description: Build queries visually in SigNoz — filter, aggregate, group, and visualize logs, traces, and metrics without writing SQL or PromQL.
doc_type: explanation
---

The Query Builder is SigNoz's visual query interface for logs, traces, and metrics. It combines expression-based filtering with intelligent auto-completion so you can query telemetry data without writing raw SQL or PromQL.

<Figure
  src="/img/docs/product-features/query-builder/product-features-query-builder.webp"
  alt="Query Builder interface in SigNoz showing filters, aggregation, and group by options"
  caption="Query Builder interface in SigNoz"
/>

## Where You Can Use Query Builder

The Query Builder is available across multiple surfaces in SigNoz:

- **Logs Explorer** — filter and aggregate log data
- **Traces Explorer** — search spans, filter by service/operation, and analyze trace data
- **Dashboards** — build custom panels with any telemetry signal
- **Alert rules** — define alert conditions on logs, traces, or metrics

## Core Capabilities

### Filtering

Filtering is the foundation of every query. The Query Builder supports expression-based filters with auto-completed attribute names, values, and operators.

**How to filter:**
1. Click the **Search Filter** field to see available attributes
2. Select an attribute (e.g., `service.name`, `severity_text`, `status_code`)
3. Choose an operator (`=`, `!=`, `IN`, `NOT_IN`, `LIKE`, `EXISTS`, etc.)
4. Enter the value to match
5. Combine multiple filters — they are joined with `AND`

<figure data-zoomable align='center'>
    <video
        className="box-shadowed-image"
        src="/img/docs/product-features/query-builder/query-builder-filtering-spans.mp4"
        width="100%"
        controls
    >
        Your browser does not support the video tag.
    </video>
    <figcaption><i>Filtering spans in the Query Builder with auto-complete</i></figcaption>
</figure>

<Admonition type="tip">
Use the keyboard shortcut `CMD+ENTER` (Mac) or `CTRL+ENTER` (Windows/Linux) to quickly execute Stage & Run Query.
</Admonition>

For the full list of supported operators and syntax patterns, see the [Operators Reference](https://signoz.io/docs/userguide/operators-reference/) and [Search Syntax](https://signoz.io/docs/userguide/search-syntax/).

### Aggregation

Aggregation functions reduce your filtered data into summary values — counts, averages, percentiles, rates, and more.

<Figure
  src="/img/docs/product-features/query-builder/aggregation.gif"
  alt="Selecting aggregation functions in the Query Builder"
  caption="Selecting an aggregation function in the Query Builder"
/>

Available aggregation functions for logs and traces:

| Category | Functions |
|----------|-----------|
| **Statistical** | `count`, `count_distinct`, `sum`, `avg`, `min`, `max` |
| **Percentile** | `p05`, `p10`, `p20`, `p25`, `p50`, `p75`, `p90`, `p95`, `p99` |
| **Rate** | `rate` (per-second), `rate_sum`, `rate_avg`, `rate_max`, `rate_min` |

For detailed usage and examples, see [Aggregation & Grouping](https://signoz.io/docs/querying/aggregation-grouping/).

### Group By

Group By segments your aggregated results by one or more attributes. This is how you break down metrics by service, endpoint, region, or any custom dimension.

<Figure
  src="/img/docs/product-features/query-builder/query-builder-group-by.webp"
  alt="Group By selector showing available attributes"
  caption="Grouping results by an attribute"
/>

For example, grouping `count` of error spans by `service.name` shows you which services produce the most errors.

### Result Manipulation

After aggregation and grouping, you can further refine results:

- **Order By** — sort results ascending or descending by any aggregated column
- **Limit** — restrict the number of results returned
- **Having** — filter aggregated results (e.g., show only services where error count > 100)

<Figure
  src="/img/docs/product-features/query-builder/query-builder-result-manipulation.gif"
  alt="Result manipulation options in the Query Builder"
  caption="Sorting, limiting, and filtering aggregated results"
/>

For a deep dive, see [Result Manipulation](https://signoz.io/docs/querying/result-manipulation/).

### Multiple Queries & Formulas

You can run multiple queries simultaneously and combine them using formulas. This is useful for computing ratios (e.g., error rate = errors / total requests) or comparing different signals side by side.

<Figure
  src="/img/docs/product-features/query-builder/query-builder-multiple-queries.gif"
  alt="Multiple queries and formulas in the Query Builder"
  caption="Combining two queries with a formula to calculate error percentage"
/>

For formula syntax and trace matching operators, see [Multi-Query Analysis](https://signoz.io/docs/querying/multi-query-analysis/).

## Metrics Query Builder

The Metrics Query Builder includes all the capabilities above — filtering, aggregation, group by, ordering — plus two additional aggregation layers that are specific to metric data: **temporal aggregation** and **spatial aggregation**.

<Figure
  src="/img/docs/product-features/query-builder/metrics-query-builder-aggregations.webp"
  alt="Temporal and spatial aggregation controls in the metrics query builder"
  caption="Temporal and spatial aggregation controls in the Metrics Query Builder"
/>

### Understanding Temporal and Spatial Aggregation

Metrics are fundamentally different from logs and traces. A single metric (e.g., `system.cpu.utilization`) produces multiple **time series** — one per unique combination of labels like `host.name`, `cpu.state`, and `service.name`. Each time series contains many **data points** over time.

When you query metrics, SigNoz applies two aggregation layers:

#### Temporal Aggregation (Within Each Time Series)

Temporal aggregation consolidates raw data points **within each individual time series** over each time interval. Instead of plotting every raw sample (which can be thousands), it reduces them into one value per interval per series.

**Think of it as:** "How do I summarize this one series over time?"

For example, if you have CPU utilization samples every 10 seconds and your chart shows 1-minute intervals, temporal aggregation decides how those 6 raw values become a single point — by taking the latest value, averaging them, summing them, etc.

Available functions: `Latest`, `Sum`, `Avg`, `Min`, `Max`, `Count`, `Count Distinct`

For counter metrics, additional rate-based functions are available: `Rate` and `Increase`.

#### Spatial Aggregation (Across Time Series)

Spatial aggregation merges **multiple time series into fewer series** by combining values across dimensions. This is how you go from seeing one line per host to seeing a single line for the entire service.

**Think of it as:** "How do I combine different series together?"

For example, if `system.cpu.utilization` has 10 time series (one per host), spatial aggregation with `Avg` gives you the average CPU across all hosts as a single line.

Available functions: `Sum`, `Avg`, `Min`, `Max`

#### How They Work Together

The two layers execute in sequence:

1. **Temporal first** — each individual time series is reduced to one value per time interval
2. **Spatial second** — the resulting time series are combined across the dimensions you didn't group by

This means temporal aggregation always happens within a single series, and spatial aggregation always happens across series — they never mix.

<Figure
  src="/img/docs/product-features/query-builder/temporal-spatial-aggregations.webp"
  alt="Diagram showing temporal and spatial aggregation flow"
  caption="Temporal aggregation reduces data points within each time series, spatial aggregation combines multiple time series"
/>

For a deeper understanding of how metric types affect which aggregations are valid, see [Metric Types and Aggregation](https://signoz.io/docs/metrics-management/types-and-aggregation/).

### Functions

The Metrics Query Builder supports additional functions that can be applied on top of your query results for extended analysis:

- **Exclusion functions** — cut off values above or below thresholds
- **Clamping functions** — constrain values to a range
- **Arithmetic functions** — absolute value, running diff, logarithms
- **Smoothing functions** — exponential weighted moving average (EWMA)
- **Time shift functions** — compare current data with a previous time period

<Figure
  src="/img/docs/product-features/query-builder/metrics-functions-dropdown.webp"
  alt="Functions dropdown in the Metrics Query Builder"
  caption="Functions available in the Metrics Query Builder"
/>

For the full list and examples, see [Functions for Extended Analysis](https://signoz.io/docs/querying/functions-extended-analysis/).

## Next Steps

- [Aggregation & Grouping](https://signoz.io/docs/querying/aggregation-grouping/) — detailed guide to aggregation functions, group-by, and legend formatting
- [Result Manipulation](https://signoz.io/docs/querying/result-manipulation/) — sort, limit, and conditionally filter aggregated results
- [Multi-Query Analysis](https://signoz.io/docs/querying/multi-query-analysis/) — formulas, trace matching operators, and cross-signal comparisons
- [Time Aggregation Windows](https://signoz.io/docs/querying/time-aggregation/) — how SigNoz calculates step intervals for time series queries
