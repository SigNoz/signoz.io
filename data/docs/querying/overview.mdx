---
date: 2026-04-10
title: Querying
id: querying-overview
description: Query logs, traces, and metrics in SigNoz using the Query Builder, ClickHouse SQL, or PromQL.
doc_type: explanation
---

## What querying looks like in SigNoz

Querying in SigNoz provides a unified interface for asking questions across all three telemetry signals — logs, traces, and metrics. Whether you prefer a visual builder, raw SQL, or Prometheus-style expressions, SigNoz supports all three approaches from a single UI.

## Key capabilities

- **Visual Query Builder**: Construct queries with point-and-click filters, aggregations, and groupings — no query language required. Autocomplete surfaces available attributes and operators as you type.
- **ClickHouse SQL**: Drop into raw SQL for arbitrary analytics over the underlying ClickHouse tables. Available for metrics, logs, and traces.
- **PromQL**: Write Prometheus-compatible queries for metric data. Useful when migrating from Prometheus or when an existing PromQL expression needs to be reused.

## Key product capabilities

### Query Builder

The visual Query Builder is available across Logs Explorer, Traces Explorer, Metrics Explorer, Dashboards, and Alerts:

- **Filters** — add attribute-level conditions with operators like `=`, `!=`, `IN`, `contains`, `exists`
- **Aggregations** — apply functions such as `count`, `sum`, `avg`, `p99`, `rate` to a filtered dataset
- **Group by** — split results by one or more attributes to compare across services, hosts, or environments
- **Expressions** — combine multiple sub-queries with mathematical formulas (e.g., `A / B * 100`) for derived metrics
- **Multi-query** — run and plot several queries side by side in a single panel

### ClickHouse SQL

ClickHouse SQL gives you full access to the underlying data model:

- Write `SELECT` statements directly against the metrics, logs, and traces tables
- Use ClickHouse-native functions for time-series rollups, string parsing, array operations, and JSON extraction
- Available in the Metrics Explorer, Dashboards panel editor, and Alert rule builder

### PromQL

PromQL support allows you to use Prometheus-style queries against metric data stored in SigNoz:

- Supports standard instant and range queries
- Compatible with rate, irate, histogram_quantile, and other common PromQL functions
- Available in the Metrics Explorer and Dashboards panel editor

## Quick links

- [Query Builder](https://signoz.io/docs/userguide/query-builder-v5/) — Build queries visually with autocomplete and expressions
- [ClickHouse](https://signoz.io/docs/userguide/write-a-metrics-clickhouse-query/) — Use SQL for advanced analysis across telemetry data
- [PromQL](https://signoz.io/docs/userguide/write-a-prom-query-with-new-format/) — Query metric time series with Prometheus-style syntax
- [Reference docs](https://signoz.io/docs/userguide/search-syntax/) — Learn search syntax, operators, functions, and field context
