---
date: 2026-05-12
title: Correlate Traces and Logs
id: correlate-traces-and-logs
description: Learn how to correlate logs and traces in SigNoz so you can navigate between them when debugging issues.
doc_type: howto
---

import GetHelp from '@/components/shared/get-help.md'

When a slow request hits, you want to jump from the trace to the exact logs that fired during that span. When an error appears in logs, you want to find the trace that produced it. Neither is possible unless your logs carry `trace_id` and `span_id`.

## How Correlation Works

SigNoz links logs to traces through two fields: `trace_id` and `span_id`. A log emitted outside any active span carries neither:

```json
{
  "timestamp": "2024-01-15T10:30:00Z",
  "severity": "ERROR",
  "body": "Payment failed",
  "service.name": "payment-service"
}
```

A log emitted inside an active span carries both, and SigNoz can connect it to the exact span that produced it:

```json
{
  "timestamp": "2024-01-15T10:30:00Z",
  "severity": "ERROR",
  "body": "Payment failed",
  "trace_id": "5b8aa5a2d2c872e8321cf37308d69df2",
  "span_id": "051581bf3cb55c13",
  "trace_flags": "01",
  "service.name": "payment-service"
}
```

The `trace_id` matches the trace in SigNoz. The `span_id` identifies which span within that trace produced the log.

## Automatic Correlation with OpenTelemetry SDK

OpenTelemetry SDKs inject `trace_id` and `span_id` into every log emitted during an active span. The setup step varies by language.

<Tabs entityName="language">
<TabItem value="python" label="Python" default>

Install the logging instrumentation package and call `.instrument()` before your app logs anything:

```bash
pip install opentelemetry-instrumentation-logging
```

```python
from opentelemetry.instrumentation.logging import LoggingInstrumentor

LoggingInstrumentor().instrument(set_logging_format=True)
```

This patches Python's standard `logging` module. Every log emitted inside a span picks up `trace_id` and `span_id` automatically.

</TabItem>
<TabItem value="java" label="Java">

**Using the Java agent (recommended)**

Attach the agent at startup. It injects `trace_id`, `span_id`, and `trace_flags` into the MDC for Logback and Log4j2 automatically. You still need those keys in your pattern layout to emit them:

```bash
java -javaagent:opentelemetry-javaagent.jar -jar myapp.jar
```

Add the MDC keys to your encoder pattern:

```
%d{HH:mm:ss.SSS} trace_id=%X{trace_id} span_id=%X{span_id} trace_flags=%X{trace_flags} %msg%n
```

**Using Logback without the agent**

Add the dependency:

```xml
<dependency>
  <groupId>io.opentelemetry.instrumentation</groupId>
  <artifactId>opentelemetry-logback-mdc-1.0</artifactId>
  <version>2.27.0-alpha</version>
</dependency>
```

Wrap your appender in `logback.xml`:

```xml
<appender name="OTEL"
  class="io.opentelemetry.instrumentation.logback.mdc.v1_0.OpenTelemetryAppender">
  <appender-ref ref="CONSOLE"/>
</appender>

<root level="INFO">
  <appender-ref ref="OTEL"/>
</root>
```

Add the MDC keys to your encoder pattern:

```
%d{HH:mm:ss.SSS} trace_id=%X{trace_id} span_id=%X{span_id} trace_flags=%X{trace_flags} %msg%n
```

For Log4j2, use `opentelemetry-log4j-context-data-2.17-autoconfigure` instead.

</TabItem>
<TabItem value="nodejs" label="Node.js">

Install the instrumentation package for your logger (Pino, Winston, or Bunyan), then register it in your SDK setup:

```bash
npm install @opentelemetry/instrumentation-winston
# or @opentelemetry/instrumentation-pino
# or @opentelemetry/instrumentation-bunyan
```

```js
const { NodeSDK } = require('@opentelemetry/sdk-node');
const { WinstonInstrumentation } = require('@opentelemetry/instrumentation-winston');

const sdk = new NodeSDK({
  instrumentations: [new WinstonInstrumentation()],
});
sdk.start();
```

The instrumentation patches the logger and adds `trace_id`, `span_id`, and `trace_flags` to each log entry when a span is active.

</TabItem>
<TabItem value="go" label="Go">

Go has no automatic context propagation for logs. Set up a logger bridge and pass `context.Context` on every log call. The bridge extracts `trace_id` and `span_id` from the active span in that context.

Initialize your `LoggerProvider` and register it globally before calling `NewLogger` — without this, no logs export:

```go
import (
  "go.opentelemetry.io/otel/log/global"
  sdklog "go.opentelemetry.io/otel/sdk/log"
)

provider := sdklog.NewLoggerProvider(/* your exporter + resource */)
global.SetLoggerProvider(provider)
```

Then create the bridge logger and pass `ctx` on every log call:

```bash
go get go.opentelemetry.io/contrib/bridges/otelslog
```

```go
import "go.opentelemetry.io/contrib/bridges/otelslog"

logger := otelslog.NewLogger("my-service")

// ctx must carry an active span
logger.InfoContext(ctx, "payment processed", "order_id", "abc123")
```

Other bridges: `otelzap` for Zap, `otellogrus` for Logrus, `otellogr` for logr.

</TabItem>
</Tabs>

See the [instrumentation docs](https://signoz.io/docs/instrumentation/) for full per-language setup.

## Manual Correlation for File-based and Kubernetes Logs

If logs go to files that a collector reads, or Kubernetes agents (FluentBit, FluentD) collect them from stdout/stderr, use SigNoz Log Pipelines to extract trace context.

Use this path when:
- Your application writes logs to files read by a collector
- Kubernetes agents collect logs and they reach SigNoz without OTel SDK injection
- Logs carry trace IDs in the message body or non-standard attributes

### Setting Up Log Pipelines

Follow the detailed guide:

**[Parse Trace Information for your Logs](https://signoz.io/docs/logs-pipelines/guides/trace/)**

It covers:
1. Creating a log pipeline in SigNoz
2. Using the Trace Parser processor to extract `trace_id` and `span_id`
3. Configuring regex or JSON parsers when needed
4. Testing and deploying your pipeline

## Verifying Correlation

1. Generate traffic in your application
2. Open **Traces** in SigNoz and click on any trace
3. Click **"Go to related logs"**
4. Check that `trace_id` and `span_id` fields appear in the matching log entries

<figure data-zoomable align='center'>
<img src="/img/docs/traces-management/correlate-traces-with-logs.gif" alt="Related logs of a trace in Logs Explorer"/>
<figcaption><i>Related logs of a trace in Logs Explorer</i></figcaption>
</figure>

You can also go the other direction: in **Logs Explorer**, filter by a specific `trace_id` to find all logs from that trace.

## Troubleshooting

**"Go to related logs" shows no results**

Your logs reach SigNoz but lack `trace_id`. You likely haven't initialized SDK logging instrumentation, or called it after loggers were already created. Move `.instrument()` (Python) or `sdk.start()` (Node.js) to before your first log statement.

**`trace_id` in logs is `00000000000000000000000000000000`**

This log fired outside an active span. Your logging calls need to sit inside instrumented request handlers, not at startup or in background goroutines with no span context.

**Logs have a `trace_id` but the trace doesn't appear in SigNoz**

Sampling dropped the trace. The log exported but the trace did not. Lower your sampling rate or switch to tail-based sampling so traces with errors always export.

**Go: logs emit but `trace_id` stays empty**

The bridge reads context from the `ctx` argument, not a global. Every log call must pass the `ctx` that holds the active span.

## Best Practices

- **Prefer OpenTelemetry SDKs** over manual pipeline parsing; auto-injection is consistent and requires no collector changes
- **Use structured logging** (JSON) so pipelines can reliably extract `trace_id` and `span_id`
- **Propagate trace context across all services** so logs from downstream calls link to the same root trace
- **Add service-level attributes** (`service.name`, `host.name`) to filter correlated logs across a distributed system

## Get Help

<GetHelp />
