---
date: 2025-04-28
id: agent-node-monitoring
title: Jenkins Host monitoring
description: Instrument Jenkins with OpenTelemetry and send traces & metrics to SigNoz
---

## Overview
This plugin integrates with the Opentelemetry Collector and Node exporter for monitoring of Jenkins build agents.

It does this by deploying the Prometheus node exporter and Opentelemetry (Otel) Collector on job agents. The Otel Collector will scrape the metrics from node exporter. 

### Prerequisites

- Jenkins [>2.479]


### Setup

#### Step 1: Install Otel agent metrics plugin
* For installation refer [here](https://plugins.jenkins.io/opentelemetry-agent-metrics/)

* Navigate to Manage Jenkins -> System -> onMonit section

<figure data-zoomable align="left">
    <img
        src="/img/docs/cicd/jenkins/agent-plugin-config.webp"
        alt="Node agent metrics plugin config"
    />
    <figcaption>
    <i>
    Node agent metrics plugin config
    </i>
    </figcaption>
</figure>

#### Step 2: Configure OTel agent metrics plugin

#### Agent node configuration required for the plugin

The plugin requires two binaries - **otel-contrib** and **node_exporter** to be installed on the jenkins agents

These executables can either be:

* present on the PATH of the agents
* downloaded from a URL on the agents. For this the global plugin option **Artifact download base URL** must be configured.
* uploaded from the Jenkins master. For this the executables must be manually stored under the JENKINS_HOME directory in the opentelemetry-agent-metrics subdirectory.

more info about this from the Jenkins plugin [docs](https://plugins.jenkins.io/opentelemetry-agent-metrics/#plugin-content-example)

<figure data-zoomable align="left">
    <img
        src="/img/docs/cicd/jenkins/agent-metrics.webp"
        alt="Jenkins agent metrics on SigNoz"
    />
    <figcaption>
    <i>
    Jenkins agent metrics on SigNoz
    </i>
    </figcaption>
</figure>

<figure data-zoomable align="left">
    <img
        src="/img/docs/cicd/jenkins/node-cpu.webp"
        alt="Node cpu metrics on SigNoz"
    />
    <figcaption>
    <i>
    Node cpu metrics on SigNoz
    </i>
    </figcaption>
</figure>

## Conclusion

This setup will allow monitoring of Jenkins agent metrics using jenkins OTel plugin on SigNoz





