---
date: 2026-08-31
title: AWS Lambda
meta_title: Monitor AWS Lambda - Logs, Metrics & Traces
description: Discover how to collect Lambda logs, metrics, and traces using OpenTelemetry layers for complete serverless function observability.
doc_type: explanation
---

<Admonition type="tip">

**SigNoz Cloud users**: You can also use the [One-Click AWS Integration](https://signoz.io/docs/integrations/aws/lambda/) for automated setup with pre-built dashboards. See the [comparison guide](https://signoz.io/docs/aws-monitoring/one-click-vs-manual/) to choose the best approach.

</Admonition>

SigNoz supports multiple ways to monitor AWS Lambda. This page helps you choose the right approach for logs, traces, and metrics, then links to detailed setup guides.

## Quick Decision Guide

| What you need                              | Recommended Method                                                                                         | Why                                                                |
| ------------------------------------------ | ---------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| **Logs** (lower latency, no code changes)  | [OTel Collector Extension Layer](https://signoz.io/docs/aws-monitoring/lambda/lambda-logs-otel-extension/) | Telemetry API integration, lower latency than S3, no code changes  |
| **Logs** (from existing S3 pipelines)      | [S3 Lambda Forwarder](https://signoz.io/docs/aws-monitoring/lambda/lambda-logs/)                           | Reuses existing S3 log storage, no changes to Lambda functions     |
| **Traces** (auto-instrumentation)          | [OTel Lambda Layers](https://signoz.io/docs/aws-monitoring/lambda/lambda-traces/)                          | Language-specific layers capture traces without code changes       |
| **Traces** (Go)                            | [Go Manual Instrumentation](https://signoz.io/docs/aws-monitoring/lambda/lambda-traces-golang/)            | Go has no auto-instrumentation layer, so it uses the OTel Go SDK    |
| **Traces** (container image functions)     | [Container Image Instrumentation](https://signoz.io/docs/aws-monitoring/lambda/lambda-traces-container-image/) | Layers do not attach to container images, so you unpack the layer into the image |
| **Metrics** (manual, low cost)             | [CloudWatch Exporter](https://signoz.io/docs/aws-monitoring/lambda/lambda-metrics/)                        | Scrapes CloudWatch metrics with minimal AWS cost                   |
| **Logs & Metrics** (fastest setup, Cloud only) | [One-Click Integration](https://signoz.io/docs/integrations/aws/lambda/)                                   | Auto-discovery, pre-built dashboards, ~5 minute setup              |
| **All signals** (Lambda MicroVMs)          | [Embedded OTel Collector](https://signoz.io/docs/aws-monitoring/lambda-microvms/)                           | MicroVMs are long-lived VMs, so they run a Collector inside the image |

## Logs: OTel Extension vs S3

### OTel Collector Extension Layer

<Figure
  src="/img/docs/aws-monitoring/lambda/lambda-logs-otel-extension-flow.svg"
  caption="OTel Collector Extension Layer flow for Lambda logs"
  alt="Architecture diagram showing Lambda function sending logs via Telemetry API to OTel Collector Extension Layer, then forwarding to SigNoz via OTLP"
/>

The OTel Collector runs as a Lambda extension and registers with Lambda's Telemetry API to receive logs automatically. No function code changes required.

**Benefits:**
- ✅ No code changes to your Lambda function
- ✅ Direct integration via Lambda Telemetry API — no CloudWatch required
- ✅ Lower latency than S3 for high-traffic functions
- ✅ Minimal AWS cost (Lambda extension overhead only)

**Trade-offs:**
- ❌ Requires adding a Lambda layer and a collector config file
- ❌ Logs are buffered — sent at the start of the **next invocation** or during function shutdown, not immediately after each invocation. For short-lived or low-traffic functions, expect visible lag.

| Feature          | Details                                  |
| ---------------- | ---------------------------------------- |
| **Setup**        | Add layer + config file                  |
| **Code Changes** | None                                     |
| **Latency**      | Next invocation or shutdown (buffered)   |
| **AWS Cost**     | Minimal (Lambda extension overhead only) |
| **Best For**     | Most use cases                           |

→ [Logs via OTel Extension Layer](https://signoz.io/docs/aws-monitoring/lambda/lambda-logs-otel-extension/)

### S3 Forwarder

<Figure
  src="/img/docs/aws-monitoring/lambda/lambda-logs-s3-flow.svg"
  caption="S3-based log forwarding flow for Lambda logs"
  alt="Architecture diagram showing Lambda logs flowing through CloudWatch to S3, then triggered Lambda forwarder sends to SigNoz"
/>

Lambda logs flow through CloudWatch Logs to an S3 bucket. A forwarder Lambda function is triggered on new objects and sends them to SigNoz. Use this when you already have a CloudWatch → S3 log archival pipeline.

**Benefits:**
- ✅ No code changes to your Lambda function
- ✅ Reuses existing S3 log storage and pipelines
- ✅ Decoupled from function execution — delivery failures don't affect your function

**Trade-offs:**
- ❌ Higher latency — delivery depends on CloudWatch → S3 export interval
- ❌ Additional AWS cost for S3 storage and forwarder Lambda invocations
- ❌ Requires setting up a separate forwarder Lambda function

| Feature          | Details                         |
| ---------------- | ------------------------------- |
| **Setup**        | Lambda function + S3 bucket     |
| **Code Changes** | None                            |
| **Latency**      | Delayed (depends on S3 sync)    |
| **AWS Cost**     | S3 storage + Lambda invocations |
| **Best For**     | Existing S3 log pipelines       |

→ [Logs via S3](https://signoz.io/docs/aws-monitoring/lambda/lambda-logs/)

## Traces: OTel Auto-Instrumentation

Add language-specific OTel layers to your Lambda function to automatically capture traces without code changes.

<Figure
  src="/img/docs/aws-monitoring/lambda/lambda-traces-flow.svg"
  caption="OTel auto-instrumentation flow for Lambda traces"
  alt="Architecture diagram showing a Lambda function with the OTel auto-instrumentation layer exporting traces to SigNoz"
/>

**Benefits:**
- ✅ No code changes — instrumentation happens via Lambda layers
- ✅ Automatically captures invocation spans and outbound HTTP calls
- ✅ Exports straight to SigNoz, with an optional OTel Collector layer for batching and enrichment

**Trade-offs:**
- ❌ Adds cold start latency from layer initialization
- ❌ Custom spans require manual instrumentation
- ❌ Trace-log correlation requires additional setup (OTel SDK logging in your function code)

**Supported languages:** Python, Node.js, Java, Ruby

→ [APM & Tracing Guide](https://signoz.io/docs/aws-monitoring/lambda/lambda-traces/)

### Traces for Go

A Go function compiles to a binary, so OpenTelemetry publishes no auto-instrumentation layer for it. Instrument your handler with the OpenTelemetry Go SDK and the `otellambda` package instead.

**Trade-offs:**
- ✅ Full control over spans and attributes, with no agent overhead on cold start
- ❌ Requires code changes in your function
- ❌ Custom spans and caller trace context need explicit wiring

→ [Go Lambda Tracing Guide](https://signoz.io/docs/aws-monitoring/lambda/lambda-traces-golang/)

### Traces for container image functions

A function with the `Image` package type cannot use Lambda layers. Unpack the OpenTelemetry
layer into the image during the Docker build instead, then set the SigNoz endpoint and ingestion key
on the function.

**Benefits:**
- ✅ The instrumentation version is pinned in the Dockerfile, next to your code

**Trade-offs:**
- ❌ Every layer upgrade needs an image rebuild and push

**Supported languages:** Python, Node.js, Java

Package Ruby functions as a .zip archive instead.

→ [Container Image Instrumentation Guide](https://signoz.io/docs/aws-monitoring/lambda/lambda-traces-container-image/)

## Metrics: CloudWatch Exporter

Scrape Lambda metrics (Invocations, Errors, Duration, Throttles) from CloudWatch using the Prometheus CloudWatch Exporter.

**Benefits:**
- ✅ No changes to Lambda functions
- ✅ Covers standard Lambda metrics out of the box
- ✅ Minimal AWS cost — reads from CloudWatch, no additional data streams

**Trade-offs:**
- ❌ Polling-based — not real-time; metrics lag by the scrape interval
- ❌ Requires running a CloudWatch Exporter instance

→ [Metrics Guide](https://signoz.io/docs/aws-monitoring/lambda/lambda-metrics/)

## Lambda MicroVMs

<a href="https://aws.amazon.com/lambda/lambda-microvms/" target="_blank" rel="noopener noreferrer nofollow">Lambda MicroVMs</a> are a separate compute primitive. Despite the name, a MicroVM behaves like a long-lived VM, closer to Fargate or EC2 than to a function, and runs for up to 8 hours.

None of the methods above apply. Layers and extensions are built around the invocation lifecycle, which a MicroVM does not have. Instead you bake an OpenTelemetry Collector into the MicroVM image, where it runs alongside your application and exports metrics, traces, and logs directly to SigNoz.

→ [Monitor AWS Lambda MicroVMs](https://signoz.io/docs/aws-monitoring/lambda-microvms/)

## One-Click vs Manual

| Feature                  | One-Click Integration           | Manual Setup                   |
| ------------------------ | ------------------------------- | ------------------------------ |
| **Availability**         | SigNoz Cloud only               | Cloud & Self-Hosted            |
| **Setup Time**           | ~5 minutes                      | 15-60 minutes                  |
| **Pre-built Dashboards** | ✅ Included                     | ❌ Build your own              |
| **Auto-Discovery**       | ✅ New resources detected       | ❌ Manual configuration        |
| **AWS Cost**             | CloudWatch + Firehose charges   | Minimal                        |
| **Granular Control**     | Limited                         | Full control                   |
| **Best For**             | Quick start, managed experience | Cost optimization, self-hosted |

See the full [One-Click vs Manual comparison](https://signoz.io/docs/aws-monitoring/one-click-vs-manual/) for details.

## Combining Approaches

Many teams use a mix:

- **One-Click** for metrics and pre-built dashboards
- **OTel Extension Layer** for lower-latency logs
- **OTel Auto-Instrumentation** for distributed traces

All data flows into the same SigNoz instance and can be queried together.
