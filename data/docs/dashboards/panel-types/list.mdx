---

title: List Chart Panel Type
date: 2026-07-29
description: Learn how to use the List Chart panel type in SigNoz dashboards to display ranked metrics data.
doc_type: reference
---

## List Chart

A List chart is a plot chart that shows a list of values. This is particularly useful when you want to show a list of values in a single panel. A typical use case is to show the error logs or the traces in a single panel. The panel pages through results and supports search.

### Data Formats

#### Supported signals

- Logs
- Traces

This panel type supports showing the logs lines and spans in a list.

#### Examples

The following graph shows the spans over a period of time in a list.

<Figure
  src="/img/docs/dashboards/panel-types/list.webp"
  alt="List panel for spans"
  caption="List panel for spans."
/>


### Configuration

<Figure
  src="/img/docs/dashboards/panel-types/list-config.webp"
  alt="List panel configuration pane in the panel editor"
  caption="List panel configuration."
/>

The columns displayed in the list are configurable. You can add, remove, and reorder columns to show the attributes most relevant to your investigation.

You can edit columns from the panel editor or directly in the expanded view. Open a List panel's action menu and select **View** to open it in the View modal, then add or remove columns without leaving the modal.

#### Paging and search

List panels page through results server-side. Use the pager at the bottom of the panel to move between pages, and the page-size picker to change how many rows each page shows.

<Admonition type="info">
The column header search filters the rows on the current page only. To search across the whole result set, narrow the panel's query instead.
</Admonition>

### Get Help

<GetHelp />